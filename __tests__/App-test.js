/**
 * @format
 */

import 'react-native';

it('does it properly', () => {
  const sum = 5 + 3;
  expect(sum).toBe(8);
});

it('does not work properly', () => {
  const sum = 5 + 3;
  expect(sum).toBe(10);
});
